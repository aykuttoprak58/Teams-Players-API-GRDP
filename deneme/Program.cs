using BusinessLayer.Concrete;
using EntityLayer;

internal class Program
{
    private static void Main(string[] args)
    {

        //TeamService tt = new TeamService();

        //var yuma = new List<Teams> { new() {TeamId=1,TeamName ="kazakistan",PlayerId=58 }};

        //foreach (var item in yuma)
        //{
        //    Console.WriteLine("no " + item.TeamId + " name " + item.TeamName + " playerno " + item.PlayerId);
        //}

    }
}